{
  "name": "gemini-translator",
  "version": "1.1.0",
  "description": "Gemini wire-format translator for the intisy-ai AI-proxy ecosystem.",
  "main": "dist/index.js",
  "type": "module",
  "license": "MIT",
  "author": "intisy",
  "repository": {
    "type": "git",
    "url": "https://github.com/intisy-ai/gemini-translator.git"
  },
  "keywords": [
    "gemini",
    "ir",
    "canonical-ir",
    "translator",
    "ai",
    "intisy"
  ],
  "files": [
    "dist/",
    "README.md",
    "LICENSE"
  ],
  "engines": {
    "node": ">=20.0.0"
  },
  "scripts": {
    "build:teavm": "node node_modules/@intisy-ai/api/scripts/teavm-build.mjs --java-dir . --module :teavm-gemini --out src/generated/gemini-translator.teavm.js",
    "build": "npm run build:teavm && tsc && node build.mjs",
    "test": "vitest run",
    "prepublishOnly": "npm run build"
  },
  "dependencies": {
    "@intisy-ai/core-ir": "^1.3.0"
  },
  "devDependencies": {
    "@intisy-ai/api": "^1.0.2",
    "@types/node": "^22.0.0",
    "esbuild": "^0.21.5",
    "typescript": "^5.6.0",
    "vitest": "^2.1.0"
  }
}
